//Ler duas variáveis numérica, e apresentar a soma das mesmas,
// acumulando ou não em outra variável.

//AREA = (BASE x ALTURA) / 2. Área de um Triângulo

algoritmo "semnome"

var
   base: real
   altura: real
   area: real
inicio
   escreva("Digite a base: ")
   leia(base)
   escreva("Digite a altura: ")
   leia(altura)
   area<- base*altura /2
   escreva("A área é: ",area)
fimalgoritmo

